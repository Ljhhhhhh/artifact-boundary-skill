interface:
  display_name: "Artifact Boundary"
  short_description: "Keep internal context out of finished artifacts"
  default_prompt: "Use $artifact-boundary to compile the current authorized target, keep internal instructions and demo scaffolding out of the finished artifact, and remove correction residue before handoff."

policy:
  allow_implicit_invocation: true
